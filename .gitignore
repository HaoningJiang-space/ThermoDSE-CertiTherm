# Submodule content - not tracked here
3d-ice/

# Python cache artifacts
__pycache__/
*.py[cod]

# Local runtime artifacts
*.log
conductance_layer.txt
.venv/
.build/
artifacts/

# Local Claude Code config — contains live API tokens, must never be committed
.claude/settings.json

# Conversation exports (/export) — these transcribe chat verbatim and have been
# observed to contain pasted credentials in plaintext. This repo is public and
# has already had one token-leak incident; never let these reach the index.
*-command-message*.txt
20??-??-??-??????-*.txt
