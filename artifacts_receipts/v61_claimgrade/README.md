# V6.1 claim-grade receipt

`v61_manifest.json` is the validated manifest of the claim-grade grid64-max factorial. It is
the ONLY source for `docs/V6_1_CAUSAL_ISOLATION.md`, which is generated by
`research/triangle/v61_render_evidence.py` and never hand-transcribed.

A consumer must check `complete is true` AND `gate.passed is true`. Neither alone suffices: an
earlier version wrote a truthy `summary` string on gate failure, so a key-existence check
would have read failure as success.

Run: commit `842e3ff`, clean tree, fresh clone, HotSpot rebuilt from the patched export,
342/342 tests, every input staged read-only and hashed as read, staged hashes re-verified
after each replay, no row reused, git state re-verified at end of run.

NOT included here: the earlier diagnostic batch under `artifacts/v61_archive/` on the remote
host. That batch produced the same 15 numbers but cannot be graded claim-grade — two drivers
ran concurrently against one output directory rewriting shared mutable inputs, and no report
recorded the hashes read at run time, so a race could not be excluded. The two must not be
cited as one kind of evidence.
