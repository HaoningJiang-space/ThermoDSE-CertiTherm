diff --git a/temperature_block.c b/temperature_block.c
--- a/temperature_block.c
+++ b/temperature_block.c
@@ -550,19 +550,19 @@ void dump_temp_block(block_model_t *model, double *temp, char *file)
 	}
 	/* on chip temperatures	*/
 	for (i=0; i < flp->n_units; i++)
-		fprintf(fp, "%s\t%.2f\n", flp->units[i].name, temp[i]);
+		fprintf(fp, "%s\t%.10f\n", flp->units[i].name, temp[i]);
 
 	/* interface temperatures	*/
 	for (i=0; i < flp->n_units; i++)
-		fprintf(fp, "iface_%s\t%.2f\n", flp->units[i].name, temp[IFACE*flp->n_units+i]);
+		fprintf(fp, "iface_%s\t%.10f\n", flp->units[i].name, temp[IFACE*flp->n_units+i]);
 
 	/* spreader temperatures	*/
 	for (i=0; i < flp->n_units; i++)
-		fprintf(fp, "hsp_%s\t%.2f\n", flp->units[i].name, temp[HSP*flp->n_units+i]);
+		fprintf(fp, "hsp_%s\t%.10f\n", flp->units[i].name, temp[HSP*flp->n_units+i]);
 
 	/* heatsink temperatures	*/
 	for (i=0; i < flp->n_units; i++)
-		fprintf(fp, "hsink_%s\t%.2f\n", flp->units[i].name, temp[HSINK*flp->n_units+i]);
+		fprintf(fp, "hsink_%s\t%.10f\n", flp->units[i].name, temp[HSINK*flp->n_units+i]);
 
 	/* internal node temperatures	*/
 	for (i=0; i < EXTRA; i++) {
@@ -567,7 +567,7 @@ void dump_temp_block(block_model_t *model, double *temp, char *file)
 	/* internal node temperatures	*/
 	for (i=0; i < EXTRA; i++) {
 		sprintf(str, "inode_%d", i);
-		fprintf(fp, "%s\t%.2f\n", str, temp[i+NL*flp->n_units]);
+		fprintf(fp, "%s\t%.10f\n", str, temp[i+NL*flp->n_units]);
 	}
diff --git a/temperature_grid.c b/temperature_grid.c
--- a/temperature_grid.c
+++ b/temperature_grid.c
@@ -1430,7 +1430,7 @@ void dump_temp_grid(grid_model_t *model, double *temp, char *file)
       }
 
       for(i=0; i < model->layers[n].flp->n_units; i++)
-        fprintf(fp, "%s%s\t%.2f\n", str,
+        fprintf(fp, "%s%s\t%.10f\n", str,
                 model->layers[n].flp->units[i].name, temp[base+i]);
       base += model->layers[n].flp->n_units;
@@ -1442,7 +1442,7 @@ void dump_temp_grid(grid_model_t *model, double *temp, char *file)
   /* internal node temperatures	*/
   for (i=0; i < extra_nodes; i++) {
       sprintf(str, "inode_%d", i);
-      fprintf(fp, "%s\t%.2f\n", str, temp[base+i]);
+      fprintf(fp, "%s\t%.10f\n", str, temp[base+i]);
   }
